class Text{
  int x;
  int y;
  color col;
  String str;
  int size;
  
  Text(int x1,int y1,String s,color c,int size){
    x=x1;
    y=y1;
    str=s;
    col=c;
    this.size=size;
  }
  
  void display(){
    fill(col);
    textAlign(CENTER,CENTER);
    textSize(size);
    text(str,x,y);
  }
}
